# Windows 本地开发可复制为 application-local.yml 后自行修改
server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/movie_mind?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: movie_mind
    password: MovieMind123!
  data:
    redis:
      host: 127.0.0.1
      port: 6379
